# TODO

开发重心将转移到系统的**规模化测试、算法鲁棒性提升以及 Demo 工程化包装**上。



## 阶段一：完善与量化进阶任务 这是进阶的任务，因为我demo中仅对一首歌匹配，还需检验评估系统的普遍可靠性

**批量构建曲库缓存**：预先读取并解析曲库中所有 `.mid` 文件的时间序列，保存为 `.npy` 或 `.pkl` 缓存文件。避免每次检索时都重新解析 MIDI，提升系统响应速度。
**扩大测试集测试**：编写一个批处理脚本，读取 `waveFile` 文件夹下多个测试者或多段哼唱，统计该系统在包含10~20首歌的曲库内的 **Top-1 命中率** 和 **Top-3 命中率**。
**多模态评价 (可选)**：针对部分噪声较大的哼唱样本，排查提取基频错判的原因，并记录不同 `energy_threshold` 环境下的准确度波动。



## 阶段二：挑战 - 核心算法鲁棒性升级
为了满足挑战任务中“提高算法对跑调、节奏不准的容错率”的要求，我们需要突破传统 DTW 的局限性：

**引入子序列 DTW (Subsequence DTW / Open-End DTW)**：
  - *当前痛点*：目前是全局 DTW (Global DTW)。如果你只哼唱了歌曲的一小段高潮（副歌），而标准 MIDI 是一整首歌，全局比对会因为两头多出来的大段空白导致距离激增而匹配失败。
  - *解决思路*：修改 DTW 算法或边界条件，允许哼唱序列与标准序列的任意中间片段对齐。
**带伴奏的旋律提取 (选做尝试环节)**：
  - *挑战内容*：如果使用包含背景音乐的 MIR-1K 数据集，现在的自相关法会失效（会被低音贝斯或鼓点带偏）。
  - *解决思路*：探索使用开源音频分离库（如 `Spleeter` 或 `Demucs`），在基频提取前加一层预处理，剥离伴奏，只保留干净的 Vocal 人声轨迹。



## 阶段三：挑战 - 终极包装为交互式 Demo (Web / GUI)
将后台冷冰冰的终端打分代码，升级为可以直观展示的图形化（GUI）应用，提升大作业表现力。

**选择快速开发框架**：推荐使用极简但绝美的 Python Web 框架，例如**Gradio** 或 **Streamlit**。
**开发录音/上传接口 (`app.py`)**：
  - 提供文件上传控件。
  - (*可选增分项*) 提供网页端直接录音麦克风的控件功能。
**集成后端逻辑并可视化反馈**：
  - 后台接收录音调用 `get_pitch_contour` 与 `calculate_similarity`。
  - 前端页面炫酷地输出 **“系统判定你唱的是: xxx”** 以及类似 `demo.py` 里那张基频对比的高清重叠图。